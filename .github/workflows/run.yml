姓名：刷新数

在：
  ＃推：
  # 分支: [ 导演 ]
  #获取请求：
  # 分支: [ 导演 ]
  足迹： 足迹： 足迹： 足迹： 足迹： 足迹：
    - cron ：'23 4,12 * * *'
  手表：
    类型：已开始
  活动调度：

工作：
  建造：
    运行：ubuntu-latest
    脚步：
      -姓名：结账代码
        用途：actions/checkout@v2
      -名称：更新系统并安装zsh
        运行：|
          sudo -E apt-get -qq 更新
          sudo -E apt-get 安装 zsh -y
          
      -姓名：初始化Python
        用途：actions/setup-python@v2
        和：
          python 版本：3.8
          
      -姓名：开始
        运行：|
          pip3 安装请求
          用户= '${{ secrets.USER }}'
          密码 = '${{ secrets.PWD }}' 
          open_get_weather= '${{ Secrets.OPEN_GET_WEATHER }}'复制代码
          区域 ='${{ secrets.AREA }}'
          python3 main.py ${user} ${passwd} ${open_get_weather} ${area}
